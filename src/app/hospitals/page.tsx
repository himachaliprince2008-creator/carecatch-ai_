import { FoundationPage } from "@/components/foundation-page";
export default function HospitalsPage() { return <FoundationPage type="file" eyebrow="Verified directory" title="Explore hospitals and care centers" description="A structured directory for comparing real facilities, services, and practical details once a verified data source is connected." />; }
